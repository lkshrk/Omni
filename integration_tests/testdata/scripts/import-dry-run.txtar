# omni tools import --dry-run with no matching provider prints "No installed tools found."
exec omni --config settings.json hosts ensure testhost
exec omni --config settings.json tools import --dry-run --provider nonexistent
stdout 'No installed tools found'

-- settings.json --
{}
