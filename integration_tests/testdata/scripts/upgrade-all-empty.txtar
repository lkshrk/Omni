# omni upgrade --all with an empty DB prints "Nothing to upgrade."
exec omni --config settings.json profile add default base
exec omni --config settings.json profile set-hostname testhost default
exec omni --config settings.json upgrade --all
stdout 'Nothing to upgrade'

-- settings.json --
{}
