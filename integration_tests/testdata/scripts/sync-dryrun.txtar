# omni tools sync --dry-run with an empty config should succeed with no ops
exec omni --config settings.json hosts ensure testhost
exec omni --config settings.json tools sync --dry-run
stdout 'Dry-run'

-- settings.json --
{}
