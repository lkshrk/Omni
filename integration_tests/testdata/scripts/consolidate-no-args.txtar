# omni tools consolidate with too few arguments exits with an error
exec omni --config settings.json hosts ensure testhost
! exec omni --config settings.json tools consolidate python
stderr 'ecosystem mode requires exactly 2 args'

-- settings.json --
{}
