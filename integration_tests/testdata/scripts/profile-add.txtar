# omni profile add creates a profile
exec omni --config settings.json profile add work base dev
stdout 'Profile "work" saved'

# profile list shows the new profile
exec omni --config settings.json profile list
stdout 'work'

-- settings.json --
{}
